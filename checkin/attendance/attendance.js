const button = document.getElementById("classlistbtn");
let selection;
button.addEventListener("click",viewClassList());

function viewClassList(event){
    var select = document.getElementById("classlist");
   return (Math.ceil(Math.random() * 100))

}
